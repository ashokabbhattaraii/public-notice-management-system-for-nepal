# Browser cache - persistent across sessions
# This directory stores puppeteer browsers via PUPPETEER_CACHE_DIR
# It is gitignored but persists locally. Do NOT delete unless you want to re-download (~400MB).
# System Chrome (/Applications/Google Chrome.app) is preferred and requires 0 download.
